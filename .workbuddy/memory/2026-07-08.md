# 2026-07-08 工作日志

## CBP Advance Ruling 爬虫项目 — 完整交付

**工作流**：⚡ 快速模式（Python CLI 爬虫工具）

**团队**：software-cbp-crawler（寇豆码工程师 + 严过关QA）

### 交付结果
- 工程师寇豆码：实现 11 个源文件，全局一致性审查 IS_PASS: YES
- QA 严过关：165/165 测试全部通过，无遗留Bug
- 项目状态：✅ ready for delivery

### 项目位置
`D:\HP\OneDrive\Desktop\学校\项目\生产实习\cbp-crawler\`

### 文件清单
| 文件 | 行数 | 功能 |
|------|------|------|
| config.py | ~90 | 全局配置 |
| utils.py | ~160 | 工具函数 |
| storage.py | ~350 | SQLite双层存储 |
| fetcher.py | ~260 | 双模驱动抓取 |
| extractor.py | ~330 | HTML解析 |
| parser.py | ~190 | 五字段解析 |
| validator.py | ~210 | 校验规则 |
| scheduler.py | ~300 | 任务队列与分片 |
| exporter.py | ~200 | 导出功能 |
| main.py | ~450 | CLI入口 |
| requirements.txt | ~25 | 依赖清单 |

### 实际运行验证 (conda py312环境)
- `pytest tests/ -v` → 165/165 passed (2.79s)
- `python main.py --help` → CLI 正常，7个参数+6种模式
- 调度器：2025年(NY+N前缀)生成194个搜索分片，任务队列正常
- DatabaseManager/Scheduler/Parser 全套模块集成连通

### 注意
- conda `agent` 环境为空（无python.exe），改用 py312 环境验证
- 项目目录下已有 `venv/`（QA创建的虚拟环境），也可直接使用
- tests/test_utils.py — 30 tests
- tests/test_storage.py — 32 tests
- tests/test_parser.py — 26 tests
- tests/test_validator.py — 26 tests
- tests/test_exporter.py — 22 tests
- tests/test_integration.py — 13 tests

## CBP 裁定查询界面 — 前端项目完整交付

**工作流**：🏗️ 标准 SOP（PRD→架构→实现→QA），设计稿阶段先审后建

**团队**：software-cbp-frontend（许清楚PM + 高见远架构师 + 寇豆码工程师 + 严过关QA）

### 交付结果
- PRD（许清楚）：简单PRD，含UI线框图+7个待确认问题
- 架构设计稿（高见远）：system_design.md + class-diagram.mermaid + sequence-diagram.mermaid
  - 关键发现：`rulings` 表当前 0 行（爬虫尚未落数），需 seed 样例数据联调
  - 决策：FastAPI后端+React前端、免登录、P0用LIKE、seed样例数据、P0带官方链接、导出筛选全集
- 工程师寇豆码：backend 14 .py + frontend 36 .ts/.tsx + seed_sample.sql（17条）
- QA 严过关：后端 pytest 45/45 + 前端 vitest 14/14 + 前端 build 通过；修复3个Bug（CSV双重BOM/非法参数500→422/Pagination命名冲突）
- 项目状态：✅ ready for delivery

### 项目位置
`D:\HP\OneDrive\Desktop\学校\项目\生产实习\cbp-ruling-explorer\`

### 关键约束（已落实）
- 后端**只读**消费爬虫SQLite（cbp-crawler/data/db/cbp_rulings.db），mode=ro + query_only=1
- 字段名严格对齐 storage.py：`is_exported`(非exported)、`hs_codes`(JSON)、`parse_failed`(INT)
- 测试库隔离：`cbp-ruling-explorer/data/test/cbp_rulings_test.db`（爬虫源库未改，仍0行）

### 启动命令
```bash
conda activate py312
pip install -r backend/requirements.txt
sqlite3 "D:/HP/OneDrive/Desktop/学校/项目/生产实习/cbp-crawler/data/db/cbp_rulings.db" < docs/seed_sample.sql
python backend/run.py          # :8000
cd frontend && npm install && npm run dev   # :5173
```

### 一键启动脚本
- 新增 `cbp-ruling-explorer/start.bat`：双击即先后台启动后端(:8000)+前端(:5173)两个独立窗口
- 内部用 `%~dp0` 定位避免中文路径编码坑；硬编码 py312 路径 `D:\Develop\miniconda\envs\py312\python.exe`，缺失时回退 PATH 的 python
- 已转 CRLF 行尾，内容全 ASCII 防 GBK 乱码
- **增强**：新增 `seed_if_empty.py` 助手，start.bat 在启动后端前自动调用；仅当 rulings 为 0 行时注入 docs/seed_sample.sql（17条），已有数据则跳过——防误清真实爬虫数据
- 已实跑验证：助手脚本对真实空库成功注入 17 条样例（rulings 现 17 行），start.bat 转 CRLF 完成
- 注：默认只 seed 爬虫默认库（cbp-crawler/data/db/cbp_rulings.db）；若 backend/.env 指向其他库需另行 seed
